language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-patternlab' ]; then cd .. && eval "mv $currentfolder generator-patternlab" && cd generator-patternlab; fi

